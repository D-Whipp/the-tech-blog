<div class="comments">
  {{#each this}}
    <section class="comment">
      {{!-- <div class="darkness"></div> --}}
      <div class="text">
        {{comment_text}}
        {{!-- |
        {{user.username}}
        on
        {{format_date created_at}} --}}
      </div>
      <div class="meta">
        {{user.username}}
        on
        {{format_date created_at}}
      </div>
    </section>
  {{/each}}
</div>